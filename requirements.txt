# Churn Prevention Agent — every external package the project imports.
# Generated by scanning every import statement in every .py and .ipynb file.
#
#   pip install -r requirements.txt

# --- the trained model and its data handling ---
# scikit-learn is PINNED. The saved model in models/churn_model.pkl was built
# with 1.6.1 in Google Colab. Loading it under a different version produces an
# InconsistentVersionWarning and, in the worst case, different numbers with
# nothing to warn you. Do not relax this pin without re-training.
scikit-learn==1.6.1
pandas
numpy
joblib

# --- the agent ---
# Talks to the language model, which runs locally via Ollama. Ollama itself is
# NOT a pip package - install it separately from ollama.com, then:
#     ollama pull qwen3:4b
ollama

# --- the chat window ---
streamlit

# --- notebook plotting (Phases 1-3 only, not needed by the agent) ---
matplotlib
